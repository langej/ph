{
    "name": "@bytethedust/ph",
    "version": "0.1.0",
    "type": "module",
    "browser": "dist/ph.js",
    "module": "dist/ph.esm.js",
    "license": "MIT",
    "devDependencies": {
        "@types/bun": "latest",
        "prettier": "^3.5.3",
        "@playwright/test": "^1.57.0"
    },
    "dependencies": {
        "@preact/signals-core": "^1.12.1"
    },
    "scripts": {
        "build": "rm -rf ./dist && bun ./build/build.ts",
        "test": "bun run build && bunx playwright test",
        "test:ui": "bun run test --ui",
        "format": "prettier --write ./src/**/*.ts",
        "test-server": "bunx http-server -p 3111"
    }
}
